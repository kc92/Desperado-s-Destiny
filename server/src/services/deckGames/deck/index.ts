/**
 * Deck Module
 * Core deck utilities and poker hand evaluation
 */

export { createDeck, drawCards, shuffleDeck } from './deckUtilities';
export { checkStraight, evaluatePokerHand, countSuitMatches, calculateSuitBonus } from './pokerHand';
